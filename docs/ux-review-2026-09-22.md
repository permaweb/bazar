# Bazar UX review — September 22, 2026

Branch: `impr/ux-prune-make-better-or-else`, based on `origin/main` at `4a877a9c`.

## Flows inspected

-   Discover, partial-name search, and search submission.
-   [Unreachable, Confirmed](https://bazar.arweave.net/#/asset/created-assets/Hmy93kQjOaASbP9fQjdCSZLzzs6vcvRgUfAsxbaeTRM): asset details and purchase cost check.
-   Global activity loading and filters.
-   Unique and token creation forms, wallet menu, and connected profile.
-   LOL token trading entry, including the state where the connected wallet owns the available listing.
-   Local desktop and 390 px mobile layouts.

## Changes

| Finding                                                                                                     | Change                                                                                                                                                                                        |
| ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Checkout reports “Compute hasn’t completed yet” for a locally rejected listing.                             | Distinguish listing fee errors from balance and network fee failures. Show the reservation minimum. Offer retries for recoverable cost checks and disable purchase with an explanatory label. |
| Searching “Unreachable” misses an asset already visible on Discover.                                        | Share up to 200 encountered assets between discovery, asset pages, the search panel, and submitted search. Keep category and visibility filters. This adds no catalogue-wide fetch.           |
| Asset details repeat price, owner, collection, supply, and technical information around the primary action. | Lead with one price, edition, fee guidance, and action. Keep verification details under Blockchain and remove repeated owner/collection facts.                                                |
| Create is an unlabeled icon on desktop and disappears on mobile.                                            | Add a desktop label and retain the accessible icon link on mobile.                                                                                                                            |
| UTF-8 punctuation in the description appears as `â…` characters.                                            | Decode display metadata from HTTP header bytes. Preserve JSON metadata and legacy Latin-1 text.                                                                                               |

## Purchase blocker: no new HyperBEAM node required

The selected node returned the asset and its open order successfully. The order is `Drn1-p4149Zsg4TvLmU3tmEOskdQPOEjXWkF6rTq9ho`, with a 2 AR asking price and a `minimum-fee` of `300000000000` winston (0.3 AR).

Bazar rejects reservation fees above `MAXIMUM_REGISTRATION_FEE`, currently `10000000000` winston (0.01 AR). The seller must replace this listing to bring it within that limit. The limit and transaction execution are unchanged. No purchase, signature, or payment was submitted during this review.

## Activity follow-up

-   Replaced the row list’s complete recipient-batched history scan with one native Arweave GraphQL page per request (`first: 100`, `HEIGHT_DESC`, edge cursor as `after`, and `pageInfo.hasNextPage`). The global feed does not depend on the provider-specific `count` extension. Queries use `arweaveGraphqlEndpoint()`, including the selected Arweave gateway override.
-   Checks marketplace membership locally instead of sending more than 16,000 recipients. Read-only gateway probes returned the unscoped recent-action page in about 0.5 seconds; large recipient predicates timed out. These measurements are observations, not latency guarantees.
-   Keeps independent query cursors for All, Listings, Transfers, and Cancellations. Shows 20 rows at a time, then loads older pages on request. Failed requests retain the last successful page and cursor. Duplicate events and stalled cursor cycles are handled. The Confirmed purchases filter and manual refresh controls are removed.
-   Activity and asset price history now load indexed events directly, with no background purchase verification, historical schedule/state scans, or verification notices. Registration events remain submissions; they are not inferred to be completed sales.
-   The row count describes loaded filter results. The feed covers currently loaded collection membership. A raw page can contain unrelated actions and yield no matching rows; it still advances the cursor and offers Check older activity. Deep generic-transfer history may need many explicit pages; the row list never automatically exhausts the global action stream.
-   Browser check: the first raw page produced 48 matching events; one older page increased this to 144. Listings independently loaded 82 matches, cancellations reached 14 and the end of that indexed stream, and returning to All retained its 144 events. Refresh returned to a fresh first page. Captured browser requests used `https://arweave.net/graphql` with native cursor variables and no count field.
-   Global charts now have an independent background reader over the full indexed history for known marketplace recipients. Their three cards show spinners until all pages finish; row filters never change the global totals. Four low-priority requests run at a time, each with at most 100 recipients and no `count` field. Background workers yield while a foreground row page loads. Completed summaries are cached for five minutes by gateway and exact asset membership. A retry resumes failed pages and retains completed batches.
-   Stats count unique indexed submissions, listings, and distinct signing wallets. They are not totals of executed transactions or confirmed sales. The scope text, completion timestamp, loaded-event description, and manual refresh buttons were removed per the screenshot feedback. Charts, filters, loading spinners, error retries, and pagination remain available. The first complete scan still takes time; cached visits reuse the finished summary. No shared backend or HyperBEAM node is required for this browser implementation.
-   Live gateway responses repeated some mined transactions as undated rows later in the same stream. Both readers now deduplicate by transaction ID while preserving known block metadata and settlement proofs. Pending events count toward totals without introducing a 1970 chart date.
-   Final browser check completed the stats scan for 18,858 marketplace assets: 2,388 unique indexed events, 133 listing submissions, and 50 signing wallets. The date range retained recent September 20 events after duplicate merging. Changing the row filter kept all three totals unchanged; leaving and returning to Activity restored the completed summary from cache with no stats loading state. The final 390 px view had no document overflow.
-   Native fields were checked against the [AR.IO GraphQL schema](https://github.com/ar-io/ar-io-node/blob/main/src/routes/graphql/schema/types.graphql). The existing research note's proposed protocol marker remains a future way to make deep historical queries more selective; this change does not alter signed transaction tags.

## Remaining observations

-   Search still depends on loaded metadata and the backend’s exact-name lookup for unseen assets. The shared index fixes consistency for encountered assets; it is not a full-text index of the entire marketplace.
-   Upload submission, seller cancellation/relisting, and completed on-chain settlement were not exercised.

## Verification

-   Full automated suite: 970 tests across 92 files after removing the five historical purchase-verifier tests.
-   Production TypeScript/Vite build, changed-file ESLint and Prettier, and `git diff --check`.
-   Local browser: selected listing’s explicit fee rejection; a second listing’s successful cost estimate, cost refresh, and insufficient-balance state; partial-name search and Enter navigation; restored mobile Create entry; corrected description; retained Blockchain verification; 390 px layout without document overflow; paged activity and independent filters; stats spinners above usable rows; background native GraphQL requests with Low priority.

The build retains its existing large-chunk warnings. Browser verification covers the review screens, not network settlement or physical-device wallet signing.

## Shared GraphQL routing

-   All app GraphQL clients share `arweaveGraphqlEndpoint()`: collection discovery, names/tokens, profiles, wallet candidates, search, activity, and the unmounted stats reader. It uses `POST <selected-Arweave-gateway>/graphql`, including `https://arweave.net/graphql` in local development. The explicit query-device route used during the initial Molecule comparison has been replaced with the standard path per the endpoint update in Nick's screenshot. Arweave gateway overrides remain independent of AO compute peers, with no Goldsky fallback.
-   Query variables, aliases, inclusion directives, and native cursor pagination are retained. Token discovery accepts native `bundledIn: { id: "" }` as unbundled while continuing to exclude actual bundle IDs.
-   Removed the historical purchase-verifier implementation and its global/asset-history callers, including proof enrichment in the activity pager. Checkout still verifies the user's own submitted transaction and live ownership; activity browsing no longer initiates that historical verification work.
-   Follow-up browser capture showed collections, carrier assets, fungible tokens, account profiles, and cursor activity queries using `https://arweave.net/graphql`. Activity and the Listings filter loaded successfully with no purchase-verification notice. A direct query returned HTTP 200 with a native `block=...&tx=...` cursor. The 970-test suite, production build, and changed-file lint/format checks passed.

## Global stats temporarily hidden

-   Removed `GlobalActivitySummary` from the activity screen per the latest request. Its background scan no longer mounts or runs. The implementation remains available for later reintroduction; activity filters, rows, and pagination are retained.
-   TypeScript and 105 focused activity/home tests passed. Browser inspection confirmed the stats cards are absent.
