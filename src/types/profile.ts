// Display-ready account profile shared by profile hooks and presentational identity components.
export type ProfileSummary = {
	address: string;
	avatar?: string;
	bio?: string;
	displayName?: string;
};
