export type { StateVerificationLabels } from './StateVerification';
export { default as StateVerification } from './StateVerification';
// The verification line's own styling, for the loading shell's placeholder copy.
export { Verification as StateVerificationLine } from './styles';
