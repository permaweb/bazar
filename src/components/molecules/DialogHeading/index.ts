export { default as DialogHeading } from './DialogHeading';
// The heading row's own styling, for dialogs that compose their title row themselves.
export { Heading as DialogHeadingRow } from './styles';
