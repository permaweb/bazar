export type { SegmentedTab } from './SegmentedTabs';
export { default as SegmentedTabs } from './SegmentedTabs';
// The tab strip's box, so a loading skeleton can render the same shape without the interactive tabs.
export { Tabs as SegmentedTabsBox } from './styles';
